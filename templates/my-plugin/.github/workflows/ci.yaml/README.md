# CI Variants

These files all target the same path relative to plugin root:
- `.github/workflows/ci.yml`

Choose one language-specific variant and copy it to the plugin root as `.github/workflows/ci.yml`.
