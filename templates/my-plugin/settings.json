{
  "note": "Copy relevant sections into .claude/settings.json, .claude/settings.local.json, or .codex/config.toml equivalents as needed.",
  "model": "gpt-5.4",
  "permissions": {
    "allow": [
      "Bash(curl:*)"
    ],
    "deny": [
      "Bash(rm -rf *)"
    ]
  },
  "hooks": {
    "enabled": true
  },
  "env": {
    "LOG_LEVEL": "info"
  }
}
